{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Administrateur list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Datederniervisite</th>
                <th>Login</th>
                <th>Motdepasse</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('administrateur_show', { 'id': entity.id }) }}">{{ entity.datederniervisite }}</a></td>
                <td>{{ entity.login }}</td>
                <td>{{ entity.motdepasse }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('administrateur_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('administrateur_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('administrateur_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
