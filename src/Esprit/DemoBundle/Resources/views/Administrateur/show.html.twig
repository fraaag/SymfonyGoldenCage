{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Administrateur</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Datederniervisite</th>
                <td>{{ entity.datederniervisite|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Login</th>
                <td>{{ entity.login }}</td>
            </tr>
            <tr>
                <th>Motdepasse</th>
                <td>{{ entity.motdepasse }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('administrateur') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('administrateur_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
